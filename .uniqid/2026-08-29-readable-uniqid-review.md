# 사람이 읽을 수 있는 고유 표식 — 병합 전 검토 발견

feature: 식별자표식
문서: .tasks/plans/2026-08-29-readable-uniqid.md

`0c2fe97` 에 대한 Claude·Codex 독립 검토의 발견. `R-`은 Claude 측, `X-`는 Codex 측이다.

| ID | 읽을 수 있는 표식 | 한 줄 설명 | 상태 |
|----|------------------|-----------|------|
| R-1 | 읽기전용-레지스트리-쓰기충돌 | 읽기 전용 계약을 가진 세 스킬에 레지스트리 쓰기를 지시해 자기 계약과 모순 | done |
| R-2 | feature-명명규칙-부재 | `feature`가 렌더형 안에 들어가는데 공백·`/` 금지 규칙이 없었음 | done |
| R-3 | 짧은형태-모호성 | 한 문서에 서로 다른 feature 의 같은 ID 가 있으면 최초 언급 후 짧은 형태가 두 대상을 합침 | done |
| R-4 | 템플릿-플레이스홀더-표기 | 통합 보고 템플릿은 `[...]`로 자리표시자를 쓰는데 `(feature/label)`만 괄호 표기 | withdrawn |
| R-5 | 레지스트리-파일명-규칙충돌 | `{yyyy-mm-dd}`는 ID 집합을 연 날인데 `{slug}`는 계획 문서와 일치하라고 해 서로 모순 | done |
| R-6 | 상태어휘-자기위반 | 레지스트리를 처음 쓰면서 닫힌 어휘에 없는 `wontfix` 를 사용 | done |
| X-1 | 계약테스트-의미반전-무방비 | 세 계약 테스트가 문자열 포함만 검사해 `Do not Write ...` 같은 반전과 HTML 주석 은닉을 통과 | done |
| X-2 | 템플릿-최초언급규칙-위반 | 템플릿이 차단 항목과 발견 제목 양쪽에 완전형을 요구해 "최초 언급만" 규칙과 충돌 | done |
| X-3 | 커밋권한-미분리 | "레지스트리를 커밋하라"가 쓰기 권한과 커밋 권한을 구분하지 않음 | done |
| X-4 | feature-불변성-미규정 | 표식만 고정한다고 하고 렌더형의 나머지 절반인 `feature` 변경은 막지 않음 | done |
| X-5 | 레지스트리간-충돌검사-부재 | 여러 `.uniqid/` 파일에서 `(feature, ID)`·`(feature, 표식)` 중복을 확인하는 절차 없음 | done |
| X-6 | 부정어목록-우회가능 | `cannot`·`may not`·`is forbidden to`·앞 문장 부정 등이 부정어 열거를 우회 | done |
| X-7 | 첫출현만-검사 | `text.index()`가 첫 출현만 봐서 두 번째 출현을 반전시켜도 통과 | done |
| X-8 | 템플릿-부분문자열-은닉 | 펜스 블록 전체 부분문자열 검색이라 `<template>` 안에 숨겨도 통과 | done |
| X-9 | 권한분리-호출자별-불일치 | 레지스트리 쓰기 권한을 세 문서가 서로 다르게 규정 | done |
| R-7 | 발행지점-반전-잔존 | 여섯 발행 지점이 느슨한 검사만 걸려 `pointless`·`obsolete`·`skip this` 로 반전 가능 | done |
| R-8 | 레지스트리행-조용한-건너뜀 | 5열이나 닫는 파이프 없는 행이 검사에서 조용히 빠짐 | done |
| R-9 | 블록범위-무한퇴행 | 앞 블록에 무효화 문장을 두면 어떤 지역 검사도 못 잡음 — 문서 전체 의미 검증은 불가 | withdrawn |
| X-10 | 브랜치리뷰-레지스트리-쓰기 | 평범한 PHP 브랜치 리뷰도 작업 트리를 바꾸면 안 되는데 등록을 지시했음 | done |
| X-11 | 다중줄-template-은닉 | 여러 줄 `<template>` 안에 기대 행을 숨기고 실제 행은 bare ID 로 두면 통과 | done |
| X-12 | 레지스트리-파서-fail-open | 이스케이프 파이프·feature 중복 선언·탭·하위 디렉터리·파일명 미검사 | done |
| X-13 | 권한규정-assertIn | 세 권한 규정이 부분 문자열 검사라 규칙을 둘러 고쳐 써도 통과 | done |
| X-14 | 참조문서-두-형식-허용 | `feature` 열 형식을 허용해 두고 검사기는 헤더 형식만 파싱 | done |
| R-10 | 비가시문자-표식-충돌 | ZWSP·소프트하이픈·BOM·bidi 마크가 공백 검사를 통과해 눈으로 같은 표식이 다른 문자열이 됨 | done |
| X-15 | 은닉컨테이너-구조적조상 | `<details>`·`<div hidden>` 로 감싸면 원문 동등 비교는 통과하지만 독자에게는 보이지 않음 | done |
| X-16 | 들여쓰기-코드블록-위장 | 4칸/탭 들여쓴 줄이 `strip()` 후 제목으로 인정됨. 중첩 `~~~` 펜스도 통과 | done |
| X-17 | 레지스트리-심볼릭링크 | `is_file()` 이 링크를 따라가 외부 대상을 검사하고 끊어진 링크는 조용히 누락 | done |
| X-18 | 전각파이프-행-무시 | `｜` 로 쓴 행은 표 행으로 인식되지 않아 통째로 검사에서 빠짐 | done |
| X-19 | feature-그림자선언 | `feature:<탭>` `feature:<NBSP>` 는 두 번째 선언으로 집계되지 않음 | done |
| X-20 | 위임경로-권한-세탁 | 리뷰가 `report-output` 에 위임하면 보고서 쓰기 권한이 레지스트리 권한으로 번짐 | done |
| X-21 | 은닉조상-개수세기-오류 | opener/closer 개수 차감이라 정상 `</div>` 가 `<details>` 를 상쇄하고, 닫힌 `aria-hidden` 은 거짓 양성 | done |
| X-22 | 고정블록-들여쓰기-무력화 | 정규화가 선행 공백을 지워 4칸 들여써 코드 블록이 돼도 동등 비교 통과 | done |
| X-23 | 외곽파이프-생략-행-누락 | `a \| b \| c \| d` 형태의 정상 마크다운 표 행이 조용히 검사에서 빠짐 | done |
| X-24 | 표행-파이프세기-취약 | 이스케이프 파이프가 있으면 파이프 개수가 임계 아래로 떨어져 행이 다시 누락 | done |
| X-25 | hidden-부분문자열-오인 | `aria-hidden="false"`·`class="not-hidden"` 를 은닉으로 오인해 정상 문서를 거부 | done |
| X-26 | css-사용자정의속성-오인 | `style="--display:none"` 는 아무것도 숨기지 않는데 은닉으로 판정 | done |
| X-27 | hidden-false-누락 | HTML 에서 `hidden="false"` 도 숨김인데 가시로 통과시킴 | done |
| X-28 | 따옴표속-꺾쇠-태그절단 | `<div title="a > b" hidden>` 이 따옴표 안 `>` 에서 잘려 `hidden` 을 못 봄 | done |
| X-29 | 수평선-표구분선-오인 | 일반 `---` 를 표 구분선으로 봐서 뒤따르는 평문을 표 행으로 취급 | done |
| X-30 | 표밖-행-무검사 | 표 본문 밖의 행 모양 줄이 어떤 검사도 받지 않음 | done |
| X-31 | 인라인css-반쪽해석 | 주석·덮어쓰기·잘못된 값을 은닉으로 오인하고 `visibility:hidden` 은 놓침 | done |
| X-32 | details-open-오인 | 펼쳐진 `<details open>` 과 닫힌 것의 `<summary>` 는 보이는데 은닉으로 판정 | done |
| X-33 | 헤더자리-데이터행 | 구분선 앞의 아무 파이프 줄이나 헤더로 인정해 데이터 행이 사라짐 | done |
| X-34 | 표밖-비표준행-누락 | 외곽 파이프 생략·전각 파이프 행이 표 밖에서 여전히 누락 | done |
| X-35 | summary-과잉면제 | `<summary>` 가 바깥의 `<div hidden>`·`<template>` 까지 무효화 | done |
| X-36 | details-속성-미검사 | `<details open hidden>` 을 가시로 보고, `class="x open y"` 의 값을 속성으로 오인 | done |
| X-37 | 헤더없는-구분선 | 헤더 없는 구분선이 표 본문을 열고, 구분선 폭이 헤더와 달라도 통과 | done |
| X-38 | 표밖-이스케이프파이프 | `(?<!\\)` 때문에 표 밖의 `\|` 행이 계속 누락 | done |
| R-11 | 은닉판정기-과잉설계 | 존재하지 않는 HTML 을 상대로 90줄짜리 조상 추적기를 만들어 6라운드 동안 계속 뚫림 | done |
| X-39 | 중앙문서-검사누락 | 규칙을 담은 `readable-ids/SKILL.md` 자신은 HTML 검사 대상이 아니었음 | done |
| X-40 | 요소이름-허용목록-누락 | `<video hidden>` 과 커스텀 요소가 목록에 없어 HTML 금지 자체가 fail-open | done |
| X-41 | 백슬래시-홀짝-미구분 | 이스케이프된 백슬래시 뒤의 파이프는 진짜 구분자인데 이스케이프로 오인해 열이 밀림 | done |
| X-42 | 닫히지않은-주석 | `-->` 를 빠뜨리면 문서 끝까지 숨겨지는데 두 검사 모두 통과 | done |
| X-43 | void요소-미검출 | `<br>`·`<img>` 는 아무것도 숨기지 않지만 'HTML 없음'에는 어긋남 | done |
